experiment_name: GradCAM Batch
run_name: default
seed: 42
verbose: true

data_contents: 'all'

model: {
  class: GradCAM,
  kwargs: {}
}
backbone: {
  class: VGG,
  kwargs: {}
}
classifier: {
  class: MLP,
  kwargs: {}
}
projection: duplicate
augment:
  - geometric
  - color
  # - blur
  # - erasing

batch_size: 32
epochs: 25
lr: 1e-3
wd: 1e-4
threshold: 0.7
